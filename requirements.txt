# Core ML dependencies
torch>=2.0.0
transformers>=4.30.0
scikit-learn>=1.3.0
numpy>=1.24.0
tqdm>=4.65.0

# Tokenizer support (required by transformers)
tokenizers>=0.13.0
sentencepiece>=0.1.99

# Optional: accelerate for faster training
# accelerate>=0.20.0
